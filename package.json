{
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "@types/node": "^24.7.2",
    "allure-commandline": "^2.34.1",
    "allure-playwright": "^3.4.1"
  },
  "scripts": {
    "test": "npx playwright test",
    "test:allure": "npx playwright test --reporter=line,allure-playwright && npx allure generate allure-results --clean && npx allure open allure-report"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}
